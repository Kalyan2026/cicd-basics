name: CI basics

on:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Run simple shell command
              run: echo "Hello from cicd pipeline"
              

